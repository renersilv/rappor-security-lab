{
  "name": "rappor-security-lab",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/compare.mjs && node --test --test-isolation=none",
    "test": "node --test --test-isolation=none"
  }
}
