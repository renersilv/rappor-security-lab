# Controlled public black-box comparison — v0.10.0

- Recorded at: `2026-09-08T18:15:22.638Z`
- Laboratory version: `0.0.0`
- Benchmark version: `1.0.0`
- Selected visible version: `v0.9.0`
- Observed visible version: `v0.10.0`
- Public contract: `v1` at `/api/public/v1/scans`
- Overall status: **partial**
- Verdict: **blocked**
- Clean: **no**

## Separate evaluation dimensions

| Dimension | Status | Sanitized result |
| --- | --- | --- |
| Detection | inconclusive | TP 0; FP 0; TN 0; FN 0; inconclusive 40 |
| Normalization | mismatch | 5 severity mismatch(es) across 26 evaluated observations |
| Grouping | not-exposed | 0/26 expected grouping keys exposed |
| Score | not-exposed | 0/4 scans exposed a score |
| Lifecycle | inconclusive | 0 matched; 0 mismatch; 10 inconclusive |
| Presentation | passed | 26/26 returned mapped items had complete presentation metadata |

## Controlled lifecycle scans

| State | Target revision | Product / coverage | Findings | Signals | Classification counts |
| --- | --- | --- | ---: | ---: | --- |
| vulnerable | `sha256:27349a558429daa88a5b9871e77144c72a2991651cf8608e7abf0bd735f51ff1` | completed / partial | 5 | 10 (4 unique) | TP 0; FP 0; TN 0; FN 0; inconclusive 10 |
| partially-fixed | `sha256:eebc2095df200cdc77eea516c1c0307a3fa7346c63efa88cb86f12f570540def` | completed / partial | 2 | 10 (4 unique) | TP 0; FP 0; TN 0; FN 0; inconclusive 10 |
| fixed | `sha256:d4bf93ce511e44dfa4246b09b606bd6f2c8a7a1e454050b5db7310773370c902` | completed / partial | 0 | 10 (4 unique) | TP 0; FP 0; TN 0; FN 0; inconclusive 10 |
| reintroduced | `sha256:e8d71517be2956a603db29e99efb7138822e1841ea6a802e5955f13c3ac077b6` | completed / partial | 5 | 10 (4 unique) | TP 0; FP 0; TN 0; FN 0; inconclusive 10 |

## Reproducible discrepancies

### deployment-visible-version

- Dimension: `presentation`
- Expected: Visible deployment version v0.9.0.
- Observed: Visible deployment version v0.10.0.
- Sanitized evidence: Only the public footer version marker was retained.
- Bounded reproduction:
  1. Use the selected public POST contract with the exact controlled target URL and accepted responsible-use terms version.
  2. Poll only the returned same-origin status path with its ephemeral token until a terminal status.
  3. Discard the token and compare sanitized rule and signal identifiers with the pinned manifest and mapping profile.

### coverage-vulnerable

- Dimension: `detection`
- Expected: Complete passive coverage is required before absence can be classified.
- Observed: The public contract reported partial coverage and the laboratory mapped the run to partial.
- Sanitized evidence: Only terminal and coverage states plus aggregate counts were retained for public-vibe-coding-vulnerable.
- Bounded reproduction:
  1. Use the selected public POST contract with the exact controlled target URL and accepted responsible-use terms version.
  2. Poll only the returned same-origin status path with its ephemeral token until a terminal status.
  3. Discard the token and compare sanitized rule and signal identifiers with the pinned manifest and mapping profile.

### coverage-partially-fixed

- Dimension: `detection`
- Expected: Complete passive coverage is required before absence can be classified.
- Observed: The public contract reported partial coverage and the laboratory mapped the run to partial.
- Sanitized evidence: Only terminal and coverage states plus aggregate counts were retained for public-vibe-coding-partially-fixed.
- Bounded reproduction:
  1. Use the selected public POST contract with the exact controlled target URL and accepted responsible-use terms version.
  2. Poll only the returned same-origin status path with its ephemeral token until a terminal status.
  3. Discard the token and compare sanitized rule and signal identifiers with the pinned manifest and mapping profile.

### coverage-fixed

- Dimension: `detection`
- Expected: Complete passive coverage is required before absence can be classified.
- Observed: The public contract reported partial coverage and the laboratory mapped the run to partial.
- Sanitized evidence: Only terminal and coverage states plus aggregate counts were retained for public-vibe-coding-fixed.
- Bounded reproduction:
  1. Use the selected public POST contract with the exact controlled target URL and accepted responsible-use terms version.
  2. Poll only the returned same-origin status path with its ephemeral token until a terminal status.
  3. Discard the token and compare sanitized rule and signal identifiers with the pinned manifest and mapping profile.

### coverage-reintroduced

- Dimension: `detection`
- Expected: Complete passive coverage is required before absence can be classified.
- Observed: The public contract reported partial coverage and the laboratory mapped the run to partial.
- Sanitized evidence: Only terminal and coverage states plus aggregate counts were retained for public-vibe-coding-reintroduced.
- Bounded reproduction:
  1. Use the selected public POST contract with the exact controlled target URL and accepted responsible-use terms version.
  2. Poll only the returned same-origin status path with its ephemeral token until a terminal status.
  3. Discard the token and compare sanitized rule and signal identifiers with the pinned manifest and mapping profile.

### severity-public-cookie-vulnerable

- Dimension: `normalization`
- Expected: Normalized severity medium.
- Observed: Normalized public result severity low.
- Sanitized evidence: The rule identifier and normalized severity were retained; title, remediation and evidence text were discarded.
- Bounded reproduction:
  1. Use the selected public POST contract with the exact controlled target URL and accepted responsible-use terms version.
  2. Poll only the returned same-origin status path with its ephemeral token until a terminal status.
  3. Discard the token and compare sanitized rule and signal identifiers with the pinned manifest and mapping profile.

### severity-public-mixed-content-vulnerable

- Dimension: `normalization`
- Expected: Normalized severity medium.
- Observed: Normalized public result severity low.
- Sanitized evidence: The rule identifier and normalized severity were retained; title, remediation and evidence text were discarded.
- Bounded reproduction:
  1. Use the selected public POST contract with the exact controlled target URL and accepted responsible-use terms version.
  2. Poll only the returned same-origin status path with its ephemeral token until a terminal status.
  3. Discard the token and compare sanitized rule and signal identifiers with the pinned manifest and mapping profile.

### severity-public-mixed-content-partially-fixed

- Dimension: `normalization`
- Expected: Normalized severity medium.
- Observed: Normalized public result severity low.
- Sanitized evidence: The rule identifier and normalized severity were retained; title, remediation and evidence text were discarded.
- Bounded reproduction:
  1. Use the selected public POST contract with the exact controlled target URL and accepted responsible-use terms version.
  2. Poll only the returned same-origin status path with its ephemeral token until a terminal status.
  3. Discard the token and compare sanitized rule and signal identifiers with the pinned manifest and mapping profile.

### severity-public-cookie-reintroduced

- Dimension: `normalization`
- Expected: Normalized severity medium.
- Observed: Normalized public result severity low.
- Sanitized evidence: The rule identifier and normalized severity were retained; title, remediation and evidence text were discarded.
- Bounded reproduction:
  1. Use the selected public POST contract with the exact controlled target URL and accepted responsible-use terms version.
  2. Poll only the returned same-origin status path with its ephemeral token until a terminal status.
  3. Discard the token and compare sanitized rule and signal identifiers with the pinned manifest and mapping profile.

### severity-public-mixed-content-reintroduced

- Dimension: `normalization`
- Expected: Normalized severity medium.
- Observed: Normalized public result severity low.
- Sanitized evidence: The rule identifier and normalized severity were retained; title, remediation and evidence text were discarded.
- Bounded reproduction:
  1. Use the selected public POST contract with the exact controlled target URL and accepted responsible-use terms version.
  2. Poll only the returned same-origin status path with its ephemeral token until a terminal status.
  3. Discard the token and compare sanitized rule and signal identifiers with the pinned manifest and mapping profile.

### lifecycle-not-proven

- Dimension: `lifecycle`
- Expected: Vulnerable, partially-fixed, fixed and reintroduced states must all have conclusive comparable coverage.
- Observed: 10 rule-family lifecycle sequence(s) are not proven by conclusive coverage.
- Sanitized evidence: The report retains only expected and observed booleans plus classifications for each pinned state.
- Bounded reproduction:
  1. Use the selected public POST contract with the exact controlled target URL and accepted responsible-use terms version.
  2. Poll only the returned same-origin status path with its ephemeral token until a terminal status.
  3. Discard the token and compare sanitized rule and signal identifiers with the pinned manifest and mapping profile.

### grouping-not-exposed

- Dimension: `grouping`
- Expected: The manifest declares stable grouping keys for detected cases.
- Observed: The selected public v1 response exposes no grouping key.
- Sanitized evidence: No product grouping identifier was inferred or retained.
- Bounded reproduction:
  1. Use the selected public POST contract with the exact controlled target URL and accepted responsible-use terms version.
  2. Poll only the returned same-origin status path with its ephemeral token until a terminal status.
  3. Discard the token and compare sanitized rule and signal identifiers with the pinned manifest and mapping profile.

### score-not-exposed

- Dimension: `score`
- Expected: Score behavior must be directly observable to support black-box comparison.
- Observed: The selected public v1 response exposes no score field.
- Sanitized evidence: No score was inferred from severity counts.
- Bounded reproduction:
  1. Use the selected public POST contract with the exact controlled target URL and accepted responsible-use terms version.
  2. Poll only the returned same-origin status path with its ephemeral token until a terminal status.
  3. Discard the token and compare sanitized rule and signal identifiers with the pinned manifest and mapping profile.

Raw response bodies, masked evidence text, remediation text, ephemeral access tokens, status URLs and internal scan identifiers were discarded. Unmapped product items are retained only by public rule or signal identifier and normalized metadata; they are not classified without independent ground truth.
